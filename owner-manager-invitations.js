(()=>{
  if(window.__onePointManagerInvitations)return;
  window.__onePointManagerInvitations=true;
  if(location.pathname.replace(/\/+$/,'')!=='/owner')return;

  const URL='https://eomgnaulupqiwjzcimqt.supabase.co',KEY='sb_publishable_p20lJcecq2HN7trRTDMW8Q_iCYVnQsM';
  const sb=window.onePointSupabase||window.supabase.createClient(URL,KEY,{auth:{persistSession:true,autoRefreshToken:true}});
  const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
  const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
  const show=(m,t='Managers',type='error')=>window.onePointMessage?window.onePointMessage(m,t,type):window.alert(m);
  const style=document.createElement('style');
  style.textContent=`.opMgrTabs{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:16px}.opMgrTab{min-height:34px!important;padding:0 12px!important;border-radius:999px!important}.opMgrTab.active{background:var(--pup-text,#1d1d1f)!important;color:#fff!important;border-color:var(--pup-text,#1d1d1f)!important}.opMgrMeta{display:flex;gap:7px;align-items:center;flex-wrap:wrap;margin-top:4px}.opMgrMeta .muted{font-size:11px}.opMgrLastActive{font-size:11px;color:var(--pup-secondary,#6e6e73);margin-top:4px}.opMgrStoreList{display:grid;gap:2px}.opMgrStoreList label{display:flex;align-items:center;gap:9px;min-height:38px;padding:7px 9px;border-radius:8px}.opMgrStoreList label:hover{background:#f7f7f8}.opMgrStoreList input{width:16px;height:16px;accent-color:var(--op-accent,#0071e3)}`;
  document.head.appendChild(style);

  let selectedTab='active',renderSeq=0,snapshot=null;
  async function context(){const{data:{session}}=await sb.auth.getSession();if(!session)throw new Error('Your Owner session has expired. Sign in again.');const{data:m,error}=await sb.from('organization_users').select('organization_id').eq('user_id',session.user.id).eq('role','owner').eq('active',true).maybeSingle();if(error)throw error;if(!m)throw new Error('Owner access is required.');return{session,orgId:m.organization_id}}
  async function invoke(c,body){const r=await fetch(`${URL}/functions/v1/invite-member`,{method:'POST',headers:{'Content-Type':'application/json','apikey':KEY,'Authorization':`Bearer ${c.session.access_token}`},body:JSON.stringify(body)}),d=await r.json().catch(()=>({}));if(!r.ok||d.error)throw new Error(d.error||'Unable to complete Manager action.');return d}
  async function audit(c,action,id,details={}){const{error}=await sb.from('audit_logs').insert({organization_id:c.orgId,actor_user_id:c.session.user.id,action,entity_type:'organization_user',entity_id:id,details});if(error)console.warn('Manager audit:',error.message)}
  function closeDrawer(){const d=$('#drawer'),b=$('#back');d?.classList.add('hidden');d?.classList.remove('modal');b?.classList.add('hidden')}
  function openDrawer(html){const d=$('#drawer'),b=$('#back');if(!d||!b)return;d.innerHTML=html;d.classList.add('modal');d.classList.remove('hidden');b.classList.remove('hidden')}
  function inviteState(m){if(m.invitation_status==='active')return{key:'active',label:'Active',remaining:''};if(m.invitation_status==='cancelled')return{key:'cancelled',label:'Cancelled',remaining:''};const invited=m.invited_at?new Date(m.invited_at).getTime():0,ms=invited+24*3600e3-Date.now();if(!invited||ms<=0)return{key:'expired',label:'Expired',remaining:''};return{key:'pending',label:'Pending',remaining:`${Math.max(1,Math.ceil(ms/3600e3))}h left`}}
  function category(m){const st=inviteState(m);if(st.key==='cancelled')return'cancelled';if(st.key==='active')return m.active?'active':'inactive';return'invitations'}
  function formatDate(v){if(!v)return'Never signed in';const d=new Date(v);return Number.isNaN(d.getTime())?'Never signed in':d.toLocaleString([],{month:'short',day:'numeric',year:'numeric',hour:'numeric',minute:'2-digit'})}
  function orderedStores(snap){const order=new Map((snap.store_order||[]).map(x=>[x.store_id,Number(x.position)])),fallback=new Map((snap.stores||[]).map((s,i)=>[s.id,i]));return[...(snap.stores||[])].sort((a,b)=>(order.has(a.id)?order.get(a.id):100000+(fallback.get(a.id)??99999))-(order.has(b.id)?order.get(b.id):100000+(fallback.get(b.id)??99999)))}
  function storesFor(m){const set=new Set(m.store_ids||[]);return orderedStores(snapshot).filter(s=>set.has(s.id)).map(s=>s.name||s.store_code)}
  function storeChecks(m=null){const assigned=new Set(m?.store_ids||[]);return`<div class="opMgrStoreList">${orderedStores(snapshot).map(s=>`<label><input type="checkbox" class="opMgrStore" value="${s.id}" ${assigned.has(s.id)?'checked':''}><span>${esc(s.name||s.store_code)}</span></label>`).join('')||'<div class="muted">Add an active location first.</div>'}</div>`}
  async function load(c){snapshot=await invoke(c,{action:'manager_snapshot',organization_id:c.orgId})}
  function row(m){const st=inviteState(m),cat=category(m),active=cat==='active',invites=cat==='invitations',names=storesFor(m);return`<div class="row"><div><b>${esc(m.display_name||m.email||'Manager')}</b><div class="opMgrMeta"><span class="muted">${esc(m.email||'')}</span><span class="muted">·</span><span class="muted">${esc(names.join(', ')||'No locations assigned')}</span></div><div class="opMgrLastActive">${active||cat==='inactive'?`Last active: ${esc(formatDate(m.last_active_at))}`:`Invitation: ${esc(st.label)}${st.remaining?' · '+esc(st.remaining):''}`}</div></div><div class="actions compact"><span class="badge ${active?'opStatusActive':st.key==='expired'||cat==='inactive'?'opStatusDanger':''}">${active?'Active':cat==='inactive'?'Inactive':esc(st.label)}</span>${active?`<button class="btn secondary opMgrEdit" data-id="${m.id}">Edit</button><button class="btn danger opMgrDeactivate" data-id="${m.id}">Deactivate</button>`:invites?`<button class="btn secondary opMgrEdit" data-id="${m.id}">Edit Invitation</button><button class="btn secondary opMgrResend" data-id="${m.id}">Resend</button><button class="btn danger opMgrCancel" data-id="${m.id}">Cancel Invitation</button>`:`<button class="btn secondary opMgrReactivate" data-id="${m.id}">Reactivate</button>`}</div></div>`}
  function draw(){const root=$('#content');if(!root)return;const all=snapshot?.managers||[],rows=all.filter(m=>category(m)===selectedTab),counts={active:0,invitations:0,inactive:0};all.forEach(m=>{const c=category(m);if(c in counts)counts[c]++});root.innerHTML=`<div class="card"><div class="head"><div></div><button class="btn primary" id="opMgrInvite">+ Invite Manager</button></div><div class="opMgrTabs"><button class="btn secondary opMgrTab ${selectedTab==='active'?'active':''}" data-tab="active">Active ${counts.active}</button><button class="btn secondary opMgrTab ${selectedTab==='invitations'?'active':''}" data-tab="invitations">Invitations ${counts.invitations}</button><button class="btn secondary opMgrTab ${selectedTab==='inactive'?'active':''}" data-tab="inactive">Inactive ${counts.inactive}</button></div><div class="list">${rows.map(row).join('')||`<div class="row"><span class="muted">No ${selectedTab==='invitations'?'pending invitations':selectedTab} managers.</span></div>`}</div></div>`;window.onePointAppleInterface?.refresh?.()}
  function managerById(id){return(snapshot?.managers||[]).find(m=>m.id===id)}
  function inviteModal(c,m=null){const pending=!!m&&category(m)==='invitations',active=!!m&&!pending;openDrawer(`<h2>${m?(pending?'Edit Manager Invitation':'Edit Manager'):'Invite Manager'}</h2><div class="field"><label>Manager Name *</label><input id="opMgrName" value="${esc(m?.display_name||'')}"></div><div class="field"><label>Email *</label><input id="opMgrEmail" type="email" value="${esc(m?.email||'')}" ${active?'disabled':''}></div><div class="field"><label>Location Access</label>${storeChecks(m)}</div><div class="actions"><button class="btn primary" id="opMgrSave">${m?(pending?'Save & Resend Invitation':'Save Changes'):'Send Invitation'}</button><button class="btn secondary" id="opMgrClose">Cancel</button></div>`);$('#opMgrClose').onclick=closeDrawer;$('#opMgrSave').onclick=async()=>{const b=$('#opMgrSave'),old=b.textContent;b.disabled=true;b.textContent=m?(pending?'Saving & Sending…':'Saving…'):'Sending…';try{const display_name=$('#opMgrName').value.replace(/\s+/g,' ').trim(),email=$('#opMgrEmail').value.trim().toLowerCase(),store_ids=$$('.opMgrStore:checked').map(x=>x.value);if(!display_name)throw new Error('Manager Name is required.');if(!email||!email.includes('@'))throw new Error('A valid Manager Email is required.');if(m)await invoke(c,{action:'update_manager',organization_id:c.orgId,membership_id:m.id,display_name,email,store_ids,resend_invitation:pending});else await invoke(c,{action:'invite_manager',organization_id:c.orgId,email,display_name,store_ids});closeDrawer();if(!m)selectedTab='invitations';show(!m?`Invitation sent to ${email}.`:pending?'Manager invitation updated and resent.':'Manager updated.','Managers','success');await render(true)}catch(e){show(e.message||String(e),'Managers')}finally{b.disabled=false;b.textContent=old}}}
  async function setManagerActive(c,id,active){const{error}=await sb.from('organization_users').update({active}).eq('id',id).eq('organization_id',c.orgId).eq('role','manager');if(error)throw error;await audit(c,active?'manager_reactivated':'manager_deactivated',id)}
  function bind(c){
    $('#opMgrInvite').onclick=()=>inviteModal(c);
    $$('.opMgrTab').forEach(b=>b.onclick=()=>{selectedTab=b.dataset.tab;render(false)});
    $$('.opMgrEdit').forEach(b=>b.onclick=()=>inviteModal(c,managerById(b.dataset.id)));
    $$('.opMgrResend').forEach(b=>b.onclick=async()=>{const old=b.textContent;b.disabled=true;b.textContent='Sending…';try{await invoke(c,{action:'resend_manager',organization_id:c.orgId,membership_id:b.dataset.id});show('A fresh 24-hour invitation was sent. The previous invitation is invalid.','Manager invitation','success');await render(true)}catch(e){show(e.message||String(e),'Manager invitation')}finally{b.disabled=false;b.textContent=old}});
    $$('.opMgrCancel').forEach(b=>b.onclick=()=>{const m=managerById(b.dataset.id);if(!confirm(`Cancel the invitation for ${m?.display_name||m?.email||'this Manager'}? The current invitation link will stop working.`))return;(async()=>{try{await invoke(c,{action:'cancel_manager',organization_id:c.orgId,membership_id:b.dataset.id});show('Manager invitation cancelled.','Managers','success');await render(true)}catch(e){show(e.message||String(e),'Managers')}})()});
    $$('.opMgrDeactivate').forEach(b=>b.onclick=async()=>{if(!confirm('Deactivate this Manager? They will lose portal access until reactivated.'))return;try{await setManagerActive(c,b.dataset.id,false);selectedTab='inactive';show('Manager deactivated.','Managers','success');await render(true)}catch(e){show(e.message||String(e),'Managers')}});
    $$('.opMgrReactivate').forEach(b=>b.onclick=async()=>{try{await setManagerActive(c,b.dataset.id,true);selectedTab='active';show('Manager reactivated.','Managers','success');await render(true)}catch(e){show(e.message||String(e),'Managers')}})
  }
  async function render(force=false){const seq=++renderSeq;try{const c=await context();if(force||!snapshot)await load(c);if(seq!==renderSeq)return;draw();bind(c)}catch(e){show(e.message||String(e),'Managers')}}
  function managersSelected(){return!!$('#nav [data-tab="managers"].active')}
  document.addEventListener('click',e=>{if(e.target.closest('#nav [data-tab="managers"]'))setTimeout(()=>{snapshot=null;render(true)},280)},true);
  setTimeout(()=>{if(managersSelected())render(true)},900);
  window.onePointManagerInvitations={refresh:()=>{snapshot=null;return render(true)}};
})();

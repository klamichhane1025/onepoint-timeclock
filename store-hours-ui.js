(()=>{
if(location.pathname.replace(/\/+$/,'')!=='/owner')return;
const URL='https://eomgnaulupqiwjzcimqt.supabase.co',KEY='sb_publishable_p20lJcecq2HN7trRTDMW8Q_iCYVnQsM';const sb=window.supabase.createClient(URL,KEY,{auth:{persistSession:true,autoRefreshToken:true}}),$=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const show=(m,t='OnePoint',type='error')=>window.onePointMessage?window.onePointMessage(m,t,type):window.alert(m);const days=['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];
async function context(){const {data:{session}}=await sb.auth.getSession();if(!session)throw new Error('Your session has expired.');const {data:m}=await sb.from('organization_users').select('organization_id,role').eq('user_id',session.user.id).eq('role','owner').eq('active',true).maybeSingle();if(!m)throw new Error('Owner access required.');return{session,orgId:m.organization_id}}
function open(html){const d=$('#drawer'),b=$('#back');d.innerHTML=html;d.classList.add('modal');d.classList.remove('hidden');b.classList.remove('hidden')}
function close(){const d=$('#drawer'),b=$('#back');d.classList.add('hidden');d.classList.remove('modal');b.classList.add('hidden')}
function inject(){for(const edit of $$('.v2EditStore')){const id=edit.dataset.id;if(edit.parentElement?.querySelector(`.v2Hours[data-id="${id}"]`))continue;const b=document.createElement('button');b.className='btn secondary v2Hours';b.dataset.id=id;b.textContent='Hours';edit.insertAdjacentElement('afterend',b);b.onclick=()=>hours(id)}}
async function hours(storeId){try{const c=await context();const [{data:store,error:sErr},{data:rows,error:rErr}]=await Promise.all([sb.from('stores').select('id,name,store_code,payroll_logic').eq('id',storeId).eq('organization_id',c.orgId).maybeSingle(),sb.from('store_hours').select('*').eq('store_id',storeId).eq('organization_id',c.orgId)]);if(sErr||rErr)throw(sErr||rErr);if(!store)throw new Error('Location not found.');const by=new Map((rows||[]).map(r=>[r.weekday,r]));open(`<h2>Operating Hours — ${store.name||store.store_code}</h2><p class="muted">Used by DFW Logic. Overnight hours are supported; for example 18:00 to 02:00.</p><div class="list">${days.map((d,i)=>{const r=by.get(i);return `<div class="row"><div style="min-width:100px"><b>${d}</b></div><label><input type="checkbox" class="hClosed" data-day="${i}" ${r?.closed?'checked':''}> Closed</label><input type="time" class="hOpen" data-day="${i}" value="${(r?.open_time||'09:00').slice(0,5)}" style="max-width:135px"><span>to</span><input type="time" class="hClose" data-day="${i}" value="${(r?.close_time||'22:00').slice(0,5)}" style="max-width:135px"></div>`}).join('')}</div><div class="actions section"><button class="btn primary" id="hSave">Save Hours</button><button class="btn secondary" id="hCancel">Cancel</button></div>`);$('#hCancel').onclick=close;const sync=()=>$$('.hClosed').forEach(ch=>{const i=ch.dataset.day;$$(`.hOpen[data-day="${i}"],.hClose[data-day="${i}"]`).forEach(x=>x.disabled=ch.checked)});$$('.hClosed').forEach(x=>x.onchange=sync);sync();$('#hSave').onclick=async()=>{const btn=$('#hSave'),old=btn.textContent;btn.disabled=true;btn.textContent='Saving…';try{const payload=days.map((_,i)=>{const closed=$(`.hClosed[data-day="${i}"]`).checked,open_time=$(`.hOpen[data-day="${i}"]`).value,close_time=$(`.hClose[data-day="${i}"]`).value;if(!closed&&(!open_time||!close_time))throw new Error(`Enter opening and closing time for ${days[i]}.`);return{organization_id:c.orgId,store_id:storeId,weekday:i,open_time:closed?null:open_time,close_time:closed?null:close_time,closed}});const {error}=await sb.from('store_hours').upsert(payload,{onConflict:'store_id,weekday'});if(error)throw error;await sb.from('audit_logs').insert({organization_id:c.orgId,actor_user_id:c.session.user.id,action:'store_hours_updated',entity_type:'store',entity_id:storeId,details:{days:7}});close();show('Operating hours saved.','Location Hours','success')}catch(e){show(e.message||String(e),'Location Hours')}finally{btn.disabled=false;btn.textContent=old}}}catch(e){show(e.message||String(e),'Location Hours')}}
document.addEventListener('click',e=>{if(e.target.closest('#nav [data-tab="stores"]')||e.target.closest('.v2EditStore,.v2DeactivateStore,.v2ReactivateStore,#v2StoreSave'))setTimeout(inject,350)},false);setTimeout(inject,1000);
})();
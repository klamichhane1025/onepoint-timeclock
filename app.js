(()=>{const $=s=>document.querySelector(s),state={route:'login',auth:null,loginRole:'owner',owners:[{id:'owner1',owner:'Krishna',business:'Krishna Retail Group',plan:'Free',status:'Active'}],managers:[{id:'manager1',name:'Store Manager',email:'manager@example.com',stores:['DFW1','DFW2'],password:'manager123'}],employees:[{id:'1047',name:'Jackie Smith',stores:['DFW1','DFW2'],job:'Cashier',payType:'hourly',rate:16,pin:'1234',status:'Active'},{id:'1051',name:'Kevin Jones',stores:['DFW2'],job:null,payType:null,rate:null,pin:'2222',status:'Active'},{id:'1063',name:'Maria Lopez',stores:['DFW1','DFW3'],job:'Manager',payType:'monthly',rate:4500,pin:'3333',status:'Active'}],stores:[{id:'DFW1',policy:'dfw',open:'09:00',close:'22:00'},{id:'DFW2',policy:'generic',open:'09:00',close:'23:00'},{id:'DFW3',policy:'dfw',open:'10:00',close:'21:00'},{id:'DFW4',policy:'generic',open:'09:00',close:'22:00'},{id:'DFW5',policy:'dfw',open:'09:00',close:'22:00'},{id:'DFW6',policy:'generic',open:'09:00',close:'22:00'}],jobs:[{name:'Cashier',type:'hourly',rate:16},{name:'Manager',type:'monthly',rate:4500}],entries:[{id:1,emp:'1047',store:'DFW1',job:'Cashier',actualIn:'2026-09-01T08:42',actualOut:'2026-09-01T17:05',payIn:'2026-09-01T09:00',payOut:'2026-09-01T17:05',edited:false,void:false},{id:2,emp:'1047',store:'DFW2',job:'Cashier',actualIn:'2026-09-02T10:03',actualOut:'2026-09-02T18:11',payIn:'2026-09-02T10:03',payOut:'2026-09-02T18:11',edited:false,void:false},{id:3,emp:'1051',store:'DFW2',job:null,actualIn:'2026-09-01T09:08',actualOut:'2026-09-01T17:02',payIn:'2026-09-01T09:08',payOut:'2026-09-01T17:02',edited:false,void:false},{id:4,emp:'1063',store:'DFW3',job:'Manager',actualIn:'2026-09-01T09:45',actualOut:'2026-09-01T21:22',payIn:'2026-09-01T10:00',payOut:'2026-09-01T21:00',edited:false,void:false},{id:5,emp:'1047',store:'DFW1',job:'Cashier',actualIn:'2026-09-03T10:17',actualOut:'2026-09-03T17:03',payIn:'2026-09-03T10:17',payOut:'2026-09-03T17:03',edited:false,void:false}],audit:[],expanded:null,kiosk:{store:'DFW1',employee:null}};
const ownerNav=[['overview','Overview'],['timesheets','Timesheets & Payroll'],['employees','Employees'],['stores','Stores'],['managers','Managers'],['jobs','Job Codes'],['kiosks','Time Clock Kiosks'],['audit','History & Audit'],['account','Account & Plan']],adminNav=[['admin','Admin Portal']];const nav=$('#nav'),content=$('#content'),title=$('#title'),who=$('#who'),sideBottom=$('#sideBottom'),toast=$('#toast');
const emp=id=>state.employees.find(x=>x.id===id),money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n||0),fmt=v=>v?new Date(v).toLocaleString('en-US',{month:'2-digit',day:'2-digit',year:'2-digit',hour:'numeric',minute:'2-digit'}):'—',minutes=e=>e.payOut&&!e.void?Math.max(0,Math.round((new Date(e.payOut)-new Date(e.payIn))/60000)):0,wage=e=>{const p=emp(e.emp);return !p||!p.payType||p.rate==null||p.payType==='monthly'?0:minutes(e)/60*Number(p.rate)};
function showToast(t){toast.textContent=t;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1600)}function allowedStores(){return state.auth?.role==='manager'?state.stores.filter(s=>state.auth.stores.includes(s.id)):state.stores}function renderNav(){const items=state.auth?.role==='admin'?adminNav:ownerNav;nav.innerHTML=state.auth?items.map(x=>`<button data-r="${x[0]}" class="${state.route===x[0]?'active':''}">${x[1]}</button>`).join(''):'';sideBottom.style.display=state.auth&&state.auth.role!=='admin'?'grid':'none'}
function render(){if(!state.auth&&state.route!=='clock')state.route='login';if(state.auth?.role==='admin'&&!['admin','login'].includes(state.route))state.route='admin';renderNav();const items=state.auth?.role==='admin'?adminNav:ownerNav;title.textContent=state.route==='login'?'Sign In':state.route==='clock'?'Employee Time Clock':items.find(x=>x[0]===state.route)?.[1]||'OnePoint';$('#eyebrow').textContent=state.auth?.role==='admin'?'ADMIN PORTAL':state.auth?'OWNER / MANAGER DASHBOARD':'ONEPOINT';who.innerHTML=state.auth?`<div class="avatar">${state.auth.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><div><b>${state.auth.name}</b><div class="muted">${state.auth.role}</div></div><button class="btn secondary" id="logout">Logout</button>`:'';if($('#logout'))$('#logout').onclick=()=>{state.auth=null;state.route='login';showToast('Logged out');render()};content.innerHTML='';({login,overview,timesheets,employees,stores,managers,jobs,kiosks,audit,account,admin,clock}[state.route]||login)()}
nav.onclick=e=>{const b=e.target.closest('button');if(b){state.route=b.dataset.r;render()}};$('#openClock').onclick=()=>{state.route='clock';render()};
function login(){content.innerHTML=`<div class="login"><div class="tabs"><button class="btn ${state.loginRole==='owner'?'primary':'secondary'}" id="ownerTab">Owner / Manager</button><button class="btn ${state.loginRole==='admin'?'primary':'secondary'}" id="adminTab">Admin</button></div><h2>${state.loginRole==='admin'?'Admin Sign In':'Owner / Manager Sign In'}</h2><p class="muted">Mock authentication only. Supabase Auth will replace this later.</p><div class="field section"><label>User ID</label><input id="loginId" placeholder="User ID"></div><div class="field"><label>Password</label><input id="loginPw" type="password" placeholder="Password"></div><button class="btn primary" id="signIn" style="width:100%;margin-top:14px">Sign In</button><div class="notice section">${state.loginRole==='admin'?'Admin demo: klamichhane / admin123':'Owner demo: owner1 / owner123 · Manager demo: manager1 / manager123'}</div></div>`;$('#ownerTab').onclick=()=>{state.loginRole='owner';login()};$('#adminTab').onclick=()=>{state.loginRole='admin';login()};$('#signIn').onclick=()=>{const id=$('#loginId').value.trim(),pw=$('#loginPw').value;if(state.loginRole==='admin'&&id==='klamichhane'&&pw==='admin123'){state.auth={role:'admin',name:'klamichhane'};state.route='admin';return render()}if(id==='owner1'&&pw==='owner123'){state.auth={role:'owner',name:'Krishna Retail Group'};state.route='overview';return render()}const m=state.managers.find(x=>x.id===id&&x.password===pw);if(m){state.auth={role:'manager',name:m.name,stores:m.stores};state.route='overview';return render()}alert('Invalid mock login. Use the demo credentials shown.')};}
function metric(l,v,s){return `<div class="card metric"><span>${l}</span><b>${v}</b><div class="muted">${s||''}</div></div>`}
function overview(){const stores=allowedStores();content.innerHTML=`<div class="grid4">${metric('Currently Clocked In',state.entries.filter(e=>!e.actualOut&&!e.void&&(state.auth.role!=='manager'||state.auth.stores.includes(e.store))).length,'Authorized locations')}${metric('Recorded Hours',state.entries.filter(e=>!e.void&&(state.auth.role!=='manager'||state.auth.stores.includes(e.store))).reduce((a,e)=>a+minutes(e)/60,0).toFixed(1),'Current mock period')}${metric('Calculated Payroll',money(state.entries.filter(e=>!e.void&&(state.auth.role!=='manager'||state.auth.stores.includes(e.store))).reduce((a,e)=>a+wage(e),0)),'Configured hourly pay only')}${metric('Locations',stores.length,'Authorized stores')}</div><div class="card section"><div class="head"><div><h2>Store Payroll Stats</h2><div class="muted">Select a store to see its payroll independently.</div></div><div class="field" style="min-width:220px"><label>Store</label><select id="dashStore"><option value="">All Stores</option>${stores.map(s=>`<option>${s.id}</option>`).join('')}</select></div></div><div id="storeStats"></div></div>`;const draw=()=>{const sid=$('#dashStore').value,rows=state.entries.filter(e=>!e.void&&(!sid||e.store===sid)&&(state.auth.role!=='manager'||state.auth.stores.includes(e.store)));$('#storeStats').innerHTML=`<div class="three"><div class="statBox"><span>Shifts</span><b>${rows.length}</b></div><div class="statBox"><span>Hours</span><b>${(rows.reduce((a,e)=>a+minutes(e),0)/60).toFixed(2)}</b></div><div class="statBox"><span>Payroll</span><b>${money(rows.reduce((a,e)=>a+wage(e),0))}</b></div></div>`};$('#dashStore').onchange=draw;draw()}
function filters(){const stores=allowedStores();return `<div class="filters"><div class="field"><label>Employee</label><select id="fEmp"><option value="">All Employees</option>${state.employees.map(e=>`<option value="${e.id}">${e.name}</option>`).join('')}</select></div><div class="field"><label>Store</label><select id="fStore"><option value="">All Stores</option>${stores.map(s=>`<option>${s.id}</option>`).join('')}</select></div><div class="field"><label>Start Date & Time</label><input id="fStart" type="datetime-local" value="2026-09-01T00:00"></div><div class="field"><label>End Date & Time</label><input id="fEnd" type="datetime-local" value="2026-09-15T23:59"></div><div class="field"><label>Job Code</label><select id="fJob"><option value="">All Job Codes</option><option value="none">No Job Code</option>${state.jobs.map(j=>`<option>${j.name}</option>`).join('')}</select></div><button class="btn primary" id="apply">Apply</button><button class="btn secondary" id="csv">Export CSV</button></div>`}
function selected(){const fe=$('#fEmp')?.value||'',fs=$('#fStore')?.value||'',fj=$('#fJob')?.value||'',st=$('#fStart')?.value||'',en=$('#fEnd')?.value||'';return state.entries.filter(e=>!e.void&&(state.auth.role!=='manager'||state.auth.stores.includes(e.store))&&(!fe||e.emp===fe)&&(!fs||e.store===fs)&&(!fj||(fj==='none'?!e.job:e.job===fj))&&(!st||e.actualIn>=st)&&(!en||e.actualIn<=en))}
function timesheets(){content.innerHTML=`<div class="card"><div class="head"><div><h2>Timesheets & Payroll</h2><div class="muted">Filters are interactive. Double-click Clock In/Out to edit.</div></div></div>${filters()}<div id="rows" class="section"></div></div>`;$('#apply').onclick=drawRows;$('#csv').onclick=exportCsv;drawRows()}
function drawRows(){const r=selected(),mins=r.reduce((a,e)=>a+minutes(e),0),hourly=r.reduce((a,e)=>a+wage(e),0),monthlyIds=[...new Set(r.filter(e=>emp(e.emp)?.payType==='monthly'&&emp(e.emp)?.rate!=null).map(e=>e.emp))],monthly=monthlyIds.reduce((a,id)=>a+Number(emp(id).rate),0);$('#rows').innerHTML=`<div class="table"><table><thead><tr><th></th><th>ID</th><th>Employee</th><th>Location</th><th>Job Code</th><th>Clock In</th><th>Clock Out</th><th>Minutes</th><th>Hours</th><th>Pay</th><th>Wages</th><th>Status</th></tr></thead><tbody>${r.map(rowHtml).join('')}</tbody></table></div><div class="summary"><div><span>Employees</span><b>${new Set(r.map(e=>e.emp)).size}</b></div><div><span>Shifts</span><b>${r.length}</b></div><div><span>Total Hours</span><b>${(mins/60).toFixed(2)}</b></div><div><span>Hourly Payroll</span><b>${money(hourly)}</b></div><div><span>Total Payroll</span><b>${money(hourly+monthly)}</b></div></div>`;document.querySelectorAll('.expand').forEach(b=>b.onclick=()=>{state.expanded=state.expanded===+b.dataset.id?null:+b.dataset.id;drawRows()});document.querySelectorAll('.edit').forEach(c=>c.ondblclick=()=>editTime(c,+c.dataset.id,c.dataset.f));document.querySelectorAll('.deleteEntry').forEach(b=>b.onclick=()=>deleteEntry(+b.dataset.id))}
function rowHtml(e){const p=emp(e.emp),m=minutes(e),pay=!p.payType||p.rate==null?'—':p.payType==='monthly'?`${money(p.rate)}/mo`:`${money(p.rate)}/hr`,w=!p.payType||p.rate==null?'—':p.payType==='monthly'?'Monthly salary':money(wage(e));return `<tr><td><button class="btn secondary expand" data-id="${e.id}" style="padding:4px 8px;min-height:30px">${state.expanded===e.id?'−':'+'}</button></td><td>${e.id}</td><td><b>${p.name}</b><div class="muted">${p.id}</div></td><td>${e.store}</td><td>${e.job||'—'}</td><td class="edit" data-id="${e.id}" data-f="actualIn">${fmt(e.actualIn)}</td><td class="edit" data-id="${e.id}" data-f="actualOut">${fmt(e.actualOut)}</td><td>${m}</td><td>${(m/60).toFixed(2)}</td><td>${pay}</td><td>${w}</td><td><span class="badge ${e.edited?'warn':'ok'}">${e.edited?'Edited':'Recorded'}</span></td></tr>${state.expanded===e.id?`<tr><td colspan="12"><div class="card" style="box-shadow:none"><b>Shift Details</b><div class="muted section">Actual: ${fmt(e.actualIn)} → ${fmt(e.actualOut)}<br>Payable: ${fmt(e.payIn)} → ${fmt(e.payOut)}</div><div class="actions"><button class="btn danger deleteEntry" data-id="${e.id}">Delete Clock Record</button></div></div></td></tr>`:''}`}
function editTime(cell,id,f){const e=state.entries.find(x=>x.id===id),old=e[f];cell.innerHTML=`<div class="inline"><input type="datetime-local" value="${old||''}" style="height:34px"><button class="btn primary save" style="min-height:34px;padding:5px 8px">Save</button></div>`;cell.querySelector('.save').onclick=()=>{const val=cell.querySelector('input').value;if(!val)return;const reason=prompt('Reason for adjustment:','Employee forgot to clock in on time');if(reason===null)return drawRows();e[f]=val;e.edited=true;const s=state.stores.find(x=>x.id===e.store);if(s.policy==='generic'){f==='actualIn'?e.payIn=val:e.payOut=val}else recalc(e);state.audit.unshift({entry:id,action:'Edited '+(f==='actualIn'?'Clock In':'Clock Out'),old:fmt(old),new:fmt(val),who:state.auth.name,reason:reason||'Manual correction'});showToast('Time updated');drawRows()}}
function recalc(e){const s=state.stores.find(x=>x.id===e.store),day=e.actualIn.slice(0,10),op=`${day}T${s.open}`,cl=`${day}T${s.close}`;e.payIn=e.actualIn<op?op:e.actualIn;if(e.actualOut)e.payOut=e.actualOut>cl?cl:e.actualOut}
function deleteEntry(id){const e=state.entries.find(x=>x.id===id);if(!confirm('Delete this clock record? It can be restored from History & Audit.'))return;e.void=true;state.audit.unshift({entry:id,action:'Deleted clock record',old:'Active',new:'Deleted',who:state.auth.name,reason:'Manual delete'});showToast('Moved to history');drawRows()}
function restoreEntry(id){const e=state.entries.find(x=>x.id===id);e.void=false;state.audit.unshift({entry:id,action:'Restored clock record',old:'Deleted',new:'Active',who:state.auth.name,reason:'Restored from history'});showToast('Clock record restored');audit()}
function exportCsv(){const h=['Entry ID','Employee ID','Employee Name','Store','Job Code','Actual Clock In','Actual Clock Out','Payable Clock In','Payable Clock Out','Minutes','Hours','Pay Type','Rate','Wages'],d=selected().map(e=>{const p=emp(e.emp);return[e.id,p.id,p.name,e.store,e.job||'',e.actualIn,e.actualOut||'',e.payIn,e.payOut||'',minutes(e),(minutes(e)/60).toFixed(2),p.payType||'',p.rate??'',p.payType==='hourly'&&p.rate!=null?wage(e).toFixed(2):'']});const txt=[h,...d].map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(',')).join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([txt],{type:'text/csv'}));a.download='onepoint-timesheet.csv';a.click();showToast('CSV exported')}
function employees(){content.innerHTML=`<div class="card"><div class="head"><div><h2>Employees</h2><div class="muted">Pay and job code are optional.</div></div>${state.auth.role==='owner'?'<button class="btn primary" id="addEmp">+ Add Employee</button>':''}</div><div class="list">${state.employees.map(e=>`<div class="row"><div><b>${e.name}</b><div class="muted">ID ${e.id} · ${e.stores.join(', ')}${e.job?' · '+e.job:''}</div></div><span class="badge ok">${e.status}</span></div>`).join('')}</div></div>`;if($('#addEmp'))$('#addEmp').onclick=()=>openDrawer(`<h2>Add Employee</h2><div class="field"><label>Name</label><input id="eName"></div><div class="field"><label>Employee ID</label><input id="eId" value="${1100+state.employees.length}"></div><div class="field"><label>PIN</label><input id="ePin" type="password"></div><div class="field"><label>Primary Store</label><select id="eStore">${state.stores.map(s=>`<option>${s.id}</option>`).join('')}</select></div><div class="field"><label>Job Code (optional)</label><select id="eJob"><option value="">None</option>${state.jobs.map(j=>`<option>${j.name}</option>`).join('')}</select></div><div class="field"><label>Pay Type (optional)</label><select id="eType"><option value="">No pay rate</option><option value="hourly">Hourly</option><option value="monthly">Monthly</option></select></div><div class="field"><label>Pay Amount (optional)</label><input id="eRate" type="number" placeholder="Leave blank"></div><div class="actions"><button class="btn primary" id="saveEmp">Create</button><button class="btn secondary" id="cancel">Cancel</button></div>`);setTimeout(()=>{$('#saveEmp').onclick=()=>{const name=$('#eName').value.trim();if(!name)return alert('Enter name');state.employees.push({id:$('#eId').value,name,stores:[$('#eStore').value],job:$('#eJob').value||null,payType:$('#eType').value||null,rate:$('#eRate').value===''?null:+$('#eRate').value,pin:$('#ePin').value||'0000',status:'Active'});closeDrawer();employees();showToast('Employee created')};$('#cancel').onclick=closeDrawer},0)}
function stores(){content.innerHTML=`<div class="card"><div class="head"><div><h2>Locations</h2><div class="muted">Owners can add locations; managers see assigned locations.</div></div>${state.auth.role==='owner'?'<button class="btn primary" id="addStore">+ Add Location</button>':''}</div><div class="two">${allowedStores().map(s=>`<div class="row"><div><b>${s.id}</b><div class="muted">${s.open}–${s.close}</div></div><span class="badge">${s.policy==='dfw'?'DFW Logic':'Generic Logic'}</span></div>`).join('')}</div></div>`;if($('#addStore'))$('#addStore').onclick=()=>{const id=prompt('Location ID',`DFW${state.stores.length+1}`);if(!id)return;state.stores.push({id,policy:'generic',open:'09:00',close:'22:00'});stores();showToast('Location added')}}
function managers(){content.innerHTML=`<div class="card"><div class="head"><div><h2>Managers</h2><div class="muted">Owners can add managers and assign stores.</div></div>${state.auth.role==='owner'?'<button class="btn primary" id="addMgr">+ Add Manager</button>':''}</div><div class="list">${state.managers.map(m=>`<div class="row"><div><b>${m.name}</b><div class="muted">${m.id} · ${m.email} · ${m.stores.join(', ')}</div></div><span class="badge ok">Active</span></div>`).join('')}</div></div>`;if($('#addMgr'))$('#addMgr').onclick=()=>{const name=prompt('Manager name','New Manager');if(!name)return;const id='manager'+(state.managers.length+1);state.managers.push({id,name,email:`${id}@example.com`,stores:[state.stores[0].id],password:'manager123'});managers();showToast('Manager added')}}
function jobs(){content.innerHTML=`<div class="card"><div class="head"><div><h2>Job Codes</h2><div class="muted">Optional hourly or monthly pay definitions.</div></div>${state.auth.role==='owner'?'<button class="btn primary" id="addJob">+ Create Job Code</button>':''}</div><div class="list">${state.jobs.map(j=>`<div class="row"><b>${j.name}</b><b>${j.type==='hourly'?money(j.rate)+'/hr':money(j.rate)+'/month'}</b></div>`).join('')}</div></div>`;if($('#addJob'))$('#addJob').onclick=()=>{const n=prompt('Job code name','Inventory');if(!n)return;const t=confirm('OK = hourly, Cancel = monthly')?'hourly':'monthly',r=+prompt('Pay amount',t==='hourly'?'18':'4000');state.jobs.push({name:n,type:t,rate:r||0});jobs();showToast('Job code created')}}
function kiosks(){content.innerHTML=`<div class="card"><div class="head"><div><h2>Time Clock Kiosks</h2><div class="muted">Authorized POS devices only expose clock actions.</div></div></div><div class="row"><div><b>${state.kiosk.store} POS Clock</b><div class="muted">Mock authorized device</div></div><span class="badge ok">Active</span></div></div>`}
function audit(){const deleted=state.entries.filter(e=>e.void);content.innerHTML=`<div class="card"><div class="head"><div><h2>Deleted Clock Records</h2><div class="muted">Restore mistakenly deleted entries here.</div></div></div><div class="list">${deleted.length?deleted.map(e=>`<div class="row history"><div><b>Entry #${e.id} · ${emp(e.emp).name}</b><div class="muted">${e.store} · ${fmt(e.actualIn)} → ${fmt(e.actualOut)}</div></div><button class="btn secondary restore" data-id="${e.id}">Restore</button></div>`).join(''):'<div class="muted">No deleted entries.</div>'}</div></div><div class="card section"><div class="head"><div><h2>Audit History</h2><div class="muted">Edits, deletions, and restores.</div></div></div><div class="list">${state.audit.length?state.audit.map(a=>`<div class="row"><div><b>Entry #${a.entry} · ${a.action}</b><div class="muted">${a.old} → ${a.new} · ${a.reason}</div></div><div class="muted">${a.who}</div></div>`).join(''):'<div class="muted">No history yet.</div>'}</div></div>`;document.querySelectorAll('.restore').forEach(b=>b.onclick=()=>restoreEntry(+b.dataset.id))}
function account(){content.innerHTML=`<div class="two"><div class="card"><h2>Account & Plan</h2><div class="summary" style="grid-template-columns:1fr 1fr"><div><span>Current Plan</span><b>Free</b></div><div><span>Stores</span><b>${state.stores.length}</b></div></div></div><div class="card"><h2>Business</h2><div class="field"><label>Business Name</label><input value="Krishna Retail Group"></div><button class="btn primary" style="margin-top:12px">Save</button></div></div>`}
function admin(){content.innerHTML=`<div class="card"><div class="head"><div><h2>Admin — Owner Accounts</h2><div class="muted">Logged in as klamichhane. Add owners and assign Free/Paid.</div></div><button class="btn primary" id="addOwner">+ Add Owner</button></div><div class="table"><table><thead><tr><th>Owner</th><th>Business</th><th>Plan</th><th>Stores</th><th>Status</th></tr></thead><tbody>${state.owners.map(o=>`<tr><td>${o.owner}</td><td>${o.business}</td><td><select class="planSel" data-id="${o.id}"><option ${o.plan==='Free'?'selected':''}>Free</option><option ${o.plan==='Paid'?'selected':''}>Paid</option></select></td><td>${o.id==='owner1'?state.stores.length:0}</td><td><span class="badge ok">${o.status}</span></td></tr>`).join('')}</tbody></table></div></div>`;$('#addOwner').onclick=()=>{const business=prompt('Business name','New Retail Group');if(!business)return;const owner=prompt('Owner name','New Owner')||'Owner';state.owners.push({id:'owner'+(state.owners.length+1),owner,business,plan:'Free',status:'Active'});admin();showToast('Owner added')};document.querySelectorAll('.planSel').forEach(s=>s.onchange=()=>{state.owners.find(o=>o.id===s.dataset.id).plan=s.value;showToast('Plan updated')})}
function clock(){const now=new Date();content.innerHTML=`<div class="clockWrap"><div class="clock"><div class="brandline">ONEPOINT TIME CLOCK · ${state.kiosk.store}</div><div class="time">${now.toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'})}</div><div class="date">${now.toLocaleDateString('en-US',{weekday:'long',month:'long',day:'numeric'})}</div>${state.kiosk.employee?clockAction():`<div class="field"><label style="text-align:left">Employee ID</label><input id="clockId" inputmode="numeric" placeholder="Employee ID"></div><div class="field"><label style="text-align:left;margin-top:10px">PIN</label><input id="clockPin" type="password" inputmode="numeric" placeholder="PIN"></div><button class="big" id="clockLogin">Continue</button><p class="muted">Employees cannot backdate missed punches.</p><p class="muted">Demo: 1047 / 1234</p>`}</div></div>`;if(state.kiosk.employee){$('#punch').onclick=()=>{const p=emp(state.kiosk.employee),open=state.entries.find(e=>e.emp===p.id&&!e.actualOut&&!e.void);if(open){open.actualOut='2026-09-03T17:05';open.payOut=open.actualOut;showToast('Clock out recorded')}else{state.entries.push({id:Math.max(...state.entries.map(e=>e.id))+1,emp:p.id,store:state.kiosk.store,job:p.job,actualIn:'2026-09-03T10:17',actualOut:null,payIn:'2026-09-03T10:17',payOut:null,edited:false,void:false});showToast('Clock in recorded')}state.kiosk.employee=null;clock()}}else $('#clockLogin').onclick=()=>{const p=state.employees.find(e=>e.id===$('#clockId').value.trim()&&e.pin===$('#clockPin').value.trim());if(!p)return alert('Try 1047 / 1234');state.kiosk.employee=p.id;clock()}}
function clockAction(){const p=emp(state.kiosk.employee),open=state.entries.find(e=>e.emp===p.id&&!e.actualOut&&!e.void);return `<div class="card" style="box-shadow:none"><h2>${p.name}</h2><p class="muted">${open?'Currently clocked in':'Currently clocked out'}</p><button class="big" id="punch">${open?'CLOCK OUT':'CLOCK IN'}</button></div>`}
function openDrawer(html){$('#drawer').innerHTML=html;$('#drawer').classList.remove('hidden');$('#back').classList.remove('hidden')}function closeDrawer(){$('#drawer').classList.add('hidden');$('#back').classList.add('hidden')}$('#back').onclick=closeDrawer;render()})();